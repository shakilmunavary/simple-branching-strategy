name: QC Pipeline

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to deploy (e.g., 1.23.1)'
        required: true

jobs:
  qc-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate release_version.txt
        run: |
          if [ ! -f release_version.txt ]; then
            echo "❌ release_version.txt not found!"
            exit 1
          fi
          VERSION=$(grep '^version:' release_version.txt | awk '{print $2}')
          DESCRIPTION=$(grep 'Description:' release_version.txt | cut -d':' -f2- | xargs)
          if [ -z "$VERSION" ] || [ -z "$DESCRIPTION" ]; then
            echo "❌ Missing version or description"
            exit 1
          fi
          echo "✅ Version: $VERSION"
          echo "✅ Description: $DESCRIPTION"

      - name: Deploy to QC
        run: |
          echo "🚀 Deploying version ${{ github.event.inputs.version }} to QC cluster..."
